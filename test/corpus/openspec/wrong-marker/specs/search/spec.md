## NEW Requirements

### Requirement: Wrong section marker
The spec delta uses `## NEW Requirements` instead of the required `## ADDED Requirements`.

#### Scenario: example
- WHEN a user does X
- THEN Y happens
