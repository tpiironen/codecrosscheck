# Tasks

## A. Implementation

- [ ] A1 Read stdin to a Buffer.
- [ ] A2 Compute SHA-256 with `node:crypto`.
- [ ] A3 Write `${hex}\n` to stdout and exit 0.

## B. Verification

- [ ] B1 Manual smoke: `echo -n hello | node sha256.js` prints the known digest.
